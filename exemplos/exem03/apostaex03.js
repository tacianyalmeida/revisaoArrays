




// if(numeros == sorteado){
// alert ('Voce acertou!');
// btnApostar.disabled = true 
// outDica.textContent = 'Parabens! ! Você acertou!'
// }else{
//     if(erros.indexOf(numero) >= 0){//verifica se existe elemento 
//         alert('Você já apostou o numero, tente outro ')
//     }else{
//         erros.push(numero)
//         let numeroErros = erros.length
//          let numeroChances = tentativas - numeroErros

//          outErros.textContent = `${numeroErros}` (${erros.join});
//          outChances.textContent = numeroChances
//          if(numeroChances === 0){
//            btnApostar.disabled = true 
//            outDica.textContent = `Voce perdeu! numero sorteado: ${sortedo}`
//         }else{
//             let dica = numero < sorteado ? 'Maior ': 'Menor'
//             outDica.textContent = `Dica: tente um numero ${dica} do que ${numero}`
//         }
//     }


// }